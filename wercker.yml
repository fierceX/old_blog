box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "HEAD"
        prod_branches: source
        dev_flags: -D -F

deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - lukevivier/master@0.2.1:
        token: $GIT_TOKEN
        domain: FierceX.github.io
        basedir: public